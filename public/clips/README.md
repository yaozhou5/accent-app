The example clip for /listen goes here, as `example.m4a` (referenced by `src="/clips/example.m4a"`).

No placeholder audio is checked in — drop the real recording in this folder before the page goes live.
